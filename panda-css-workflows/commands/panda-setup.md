---
description: Set up Panda CSS in a React + Vite project with configuration and build integration
---

Use the Skill tool to invoke the panda-setup-config skill:

"panda-css-workflows:panda-setup-config"

This skill will guide you through:
- Installing and initializing Panda CSS
- Configuring panda.config.ts with best practices
- Setting up Vite aliases and TypeScript paths
- Creating preset architecture for design systems
- Build script integration
